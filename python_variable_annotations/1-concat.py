#!/usr/bin/env python3
"""This module contains a type-annotated function to concatenate two strings."""


def concat(str1: str, str2: str) -> str:
    """Concatenate two strings and return the combined string."""
    return str1 + str2
